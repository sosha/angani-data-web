<?php
session_start();
require __DIR__ . '/includes/db.php';
require __DIR__ . '/includes/functions.php';

try { handle_post_actions(); } catch (Throwable $e) { flash('error', $e->getMessage()); }
if (getv('page') === 'logout') { logout_user(); redirect_to('?page=home'); }
if (getv('page') === 'export') { export_module_csv(getv('module')); }
if (getv('page') === 'export_all') { export_database_zip(); }

$page = getv('page','home');
$moduleFromPage = module_key_from_page($page);
if ($moduleFromPage) { $_GET['module'] = $moduleFromPage; $page='module'; }
$valid = ['home','search','catalogue','module','detail','pricing','login','register','dashboard','answer','account','admin','contact'];
if (!in_array($page,$valid,true)) $page='home';
$user=current_user(); $dbError=null; $stats=[];
try { $stats=get_stats(); } catch(Throwable $e) { $dbError=$e->getMessage(); }
?>
<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><title>Angani Data — Aviation Intelligence Atlas</title><meta name="description" content="A PHP/MySQL aviation data platform for airlines, airports, aircraft, routes, infrastructure, regulatory and commercial datasets."><link rel="icon" href="assets/favicon.png" type="image/png"><link rel="stylesheet" href="css/styles.css"></head>
<body class="<?= $page==='admin'?'admin-shell-page':'' ?>"><div class="flight-grid" aria-hidden="true"></div>
<header class="site-header"><a class="brand-lockup" href="?page=home"><img src="assets/angani-logo-white.png" alt="Angani" class="brand-logo"><span><strong>Angani Data</strong><small>Aviation Intelligence Atlas</small></span></a><button class="menu-toggle" id="menuToggle" aria-expanded="false" aria-controls="mainNav">Menu</button><nav class="main-nav" id="mainNav"><a class="<?=active_page($page,'home')?>" href="?page=home">Overview</a><a class="<?=active_page($page,'search')?>" href="?page=search">Search</a><a class="<?=active_page($page,'catalogue')?>" href="?page=catalogue">Databases</a><a href="?page=airlines">Airlines</a><a href="?page=airports">Airports</a><a href="?page=aircraft_types">Aircraft Types</a><a href="?page=reference">Reference</a><a class="<?=active_page($page,'pricing')?>" href="?page=pricing">Pricing</a><?php if($user): ?><a class="<?=active_page($page,'dashboard')?>" href="?page=dashboard">Dashboard</a><?php if(is_admin()): ?><a class="<?=active_page($page,'admin')?>" href="?page=admin">Admin</a><?php endif; ?><a href="?page=account"><?=tier_badge($user)?></a><?php else: ?><a class="<?=active_page($page,'login')?>" href="?page=login">Log in</a><?php endif; ?></nav></header>
<main id="app"><?=flash_html()?>
<?php if($dbError): ?>
<section class="view"><div class="empty-state"><h2>Database connection required</h2><p><?=e($dbError)?></p><p>Import <code>database/00_create_database.sql</code>, then <code>database/01_schema.sql</code>, then run <code>php database/import_all_seeds.php</code>.</p></div></section>
<?php else: ?>
<?php if($page === 'home'): $insights = active_insight_cards(); $regions=rows("SELECT COALESCE(region,'Other') region, COUNT(*) total FROM airlines WHERE country_code <> 'GLOBAL' GROUP BY COALESCE(region,'Other') ORDER BY total DESC LIMIT 8"); ?>
<section class="view home-view">
    <div class="hero premium-hero">
        <div>
            <div class="kicker">Angani Solutions · Aviation Data Product</div>
            <h1>Aviation data arranged for intelligent decisions.</h1>
            <p class="lead">Airlines, aircraft, airports, routes, lessors, regulatory bodies and key people in one aviation-grade PHP/MySQL product. Built for research, due diligence, market entry, route planning and board-ready analysis.</p>
            <div class="hero-actions"><a class="btn primary" href="?page=register">Create account</a><a class="btn secondary" href="?page=insights">View data titbits</a><a class="btn secondary" href="?page=pricing">Compare tiers</a></div>
        </div>
        <aside class="hero-panel cockpit-panel">
            <img src="assets/angani-logo-white.png" alt="Angani" class="mini-logo">
            <h2>Reference data, source tracking and controlled history.</h2>
            <p>The app now includes accounts, tier-gated access, preset intelligence questions, route/equipment models and admin-managed homepage insights.</p>
        </aside>
    </div>
    <div class="stats-grid">
        <?=metric_card(nfmt($stats['airlines']),'Airline records',nfmt($stats['active_airlines']).' active records')?>
        <?=metric_card(nfmt($stats['airports']),'Airport records','Profiles, codes, elevation and location')?>
        <?=metric_card(nfmt($stats['aircraft']),'Aircraft registrations','Tail records with age and operator fields')?>
        <?=metric_card(nfmt($stats['routes']),'Route services','Schedules, equipment and competitors')?></div>

    <section class="section-head"><div><div class="eyebrow">Rotating homepage intelligence</div><h2>Fresh reasons to sign up</h2></div><p>These homepage cards are controlled from the admin area. Each card can point to a query such as oldest aircraft, highest airports, competitive routes or regulatory depth.</p></section>
    <div class="insight-grid">
    <?php foreach($insights as $card): $rows=run_insight_query($card['query_key']); $locked = $card['required_tier_id'] && !meets_tier((int)$card['required_tier_id']); ?>
        <article class="insight-card <?= $locked ? 'locked' : '' ?>">
            <div class="topline"><span class="chip gold"><?=e($card['metric_label'])?></span><?php if($locked): ?><span class="chip danger">Upgrade</span><?php endif; ?></div>
            <h3><?=e($card['title'])?></h3><p><?=e($card['description'])?></p>
            <?php if($locked): ?><?=access_gate('Tier insight locked','This public card exists to preview the data product. Sign in or upgrade to unlock the detail.','See pricing')?><?php else: ?><?=chart_bars($rows)?><?php endif; ?>
        </article>
    <?php endforeach; ?>
    </div>

    <section class="section-head"><div><div class="eyebrow">Coverage map</div><h2>Database coverage by region</h2></div><p>The public landing page gives visitors a credible taste without exposing every advanced field.</p></section>
    <div class="panel-grid"><div class="panel"><h3>Airline distribution</h3><?=chart_bars(array_map(fn($r)=>['label'=>$r['region'],'detail'=>'Airline records','value'=>$r['total']], $regions))?></div><div class="panel dark"><h3>What changed in this build?</h3><ul class="clean-list"><li>User accounts and session login</li><li>Tier definitions and gated features</li><li>Preset questions after login</li><li>Route, schedule and competitor model</li><li>Admin-managed data titbits</li><li>Source and change-control tables</li></ul></div></div>
</section>
<?php elseif($page === 'insights'): ?>
<section class="view"><section class="section-head"><div><div class="eyebrow">Data titbits</div><h2>Public intelligence samples</h2></div><p>These are the chartable, regularly rotated data snippets that make the site feel alive while encouraging account creation.</p></section><div class="insight-grid"><?php foreach(active_insight_cards() as $card): ?><article class="insight-card"><div class="topline"><span class="chip gold"><?=e($card['metric_label'])?></span><span class="chip"><?=e($card['chart_type'])?></span></div><h3><?=e($card['title'])?></h3><p><?=e($card['description'])?></p><?=chart_bars(run_insight_query($card['query_key']))?></article><?php endforeach; ?></div></section>
<?php elseif($page === 'pricing'): $tiers=tier_cards(); ?>
<section class="view"><section class="section-head"><div><div class="eyebrow">Access model</div><h2>Tiers and benefits</h2></div><p>The product is structured for a freemium funnel: public evidence, free account exploration, paid analyst tools, then API/export/team access.</p></section><div class="pricing-grid"><?php foreach($tiers as $tier): ?><article class="pricing-card <?= $tier['code']==='pro'?'featured':'' ?>"><div class="topline"><span class="chip gold"><?=e($tier['name'])?></span><span class="chip">$<?=e($tier['monthly_usd'])?>/mo</span></div><h3><?=e($tier['name'])?></h3><p><?=e($tier['description'])?></p><div class="price">$<?=e($tier['monthly_usd'])?><small>/month</small></div><ul><?php foreach(tier_features((int)$tier['id']) as $f): ?><li><?=e($f['feature_label'])?></li><?php endforeach; ?></ul><a class="btn <?= $tier['code']==='pro'?'primary':'ghost' ?>" href="?page=register">Start with <?=e($tier['name'])?></a></article><?php endforeach; ?></div></section>
<?php elseif($page === 'login'): ?>
<section class="auth-shell"><div class="panel auth-card"><div class="eyebrow">Member access</div><h1>Log in</h1><p class="muted">Demo admin: <code>admin@angani.co.uk</code> / <code>Angani@2026</code></p><form method="post" class="stack-form"><?=csrf_field()?><input type="hidden" name="action" value="login"><label>Email<input name="email" type="email" required></label><label>Password<input name="password" type="password" required></label><button class="btn ink" type="submit">Log in</button></form><p>New user? <a class="linkish" href="?page=register">Create an account</a></p></div></section>
<?php elseif($page === 'register'): ?>
<section class="auth-shell"><div class="panel auth-card"><div class="eyebrow">Create account</div><h1>Start exploring</h1><form method="post" class="stack-form"><?=csrf_field()?><input type="hidden" name="action" value="register"><label>Name<input name="name" required></label><label>Email<input name="email" type="email" required></label><label>Password<input name="password" type="password" minlength="8" required></label><button class="btn ink" type="submit">Create account</button></form><p>Already have an account? <a class="linkish" href="?page=login">Log in</a></p></div></section>
<?php elseif($page === 'dashboard'): if(!$user): echo access_gate('Log in to use the intelligence cockpit','Preset questions, saved searches and deeper dataset access require a user account.'); else: $presets=question_presets(); ?>
<section class="view"><div class="dashboard-hero"><div><div class="eyebrow">Your cockpit</div><h1>Welcome, <?=e($user['name'])?></h1><p>You are on the <strong><?=e($user['tier_name'])?></strong> tier. Use preset questions to interrogate the data without memorising table names.</p></div><div class="dashboard-tier"><?=tier_badge($user)?><a class="btn secondary" href="?page=pricing">Upgrade path</a></div></div><section class="section-head"><div><div class="eyebrow">Preset questions</div><h2>Questions this dataset should answer</h2></div><p>These presets guide users into useful aviation workflows immediately after login. Locked cards show the business reason to upgrade.</p></section><div class="question-grid"><?php foreach($presets as $q): $locked=!meets_tier((int)$q['required_tier_id']); ?><article class="question-card <?= $locked?'locked':'' ?>"><span class="chip gold"><?=e($q['category'])?></span><h3><?=e($q['title'])?></h3><p><?=e($q['question_text'])?></p><div class="topline"><span class="chip">Requires <?=e($q['required_tier_name'] ?: 'Free')?></span><?php if($locked): ?><a class="btn ghost" href="?page=pricing">Upgrade</a><?php else: ?><a class="btn ink" href="?page=answer&id=<?=e($q['id'])?>">Answer this</a><?php endif; ?></div></article><?php endforeach; ?></div></section><?php endif; ?>
<?php elseif($page === 'answer'): if(!$user): echo access_gate('Log in to answer questions','The preset intelligence layer is available after login.'); else: $id=(int)getv('id'); $preset=row('SELECT qp.*, st.name required_tier_name FROM question_presets qp LEFT JOIN subscription_tiers st ON st.id=qp.required_tier_id WHERE qp.id=?',[$id]); if(!$preset): ?><div class="empty-state"><h2>Question not found</h2></div><?php elseif(!meets_tier((int)$preset['required_tier_id'])): echo access_gate('Upgrade required','This question requires the '.$preset['required_tier_name'].' tier.'); else: $results=run_preset_query($preset['answer_key']); ?><section class="view"><a class="btn ghost" href="?page=dashboard">← Back to dashboard</a><section class="section-head"><div><div class="eyebrow"><?=e($preset['category'])?></div><h2><?=e($preset['title'])?></h2></div><p><?=e($preset['question_text'])?></p></section><div class="panel"><?=render_result_table($results)?></div></section><?php endif; endif; ?>
<?php elseif($page === 'airlines'): $q=getv('q');$status=getv('status','active');$region=getv('region','all');$country=getv('country','all');$per=36;$offset=(page_num()-1)*$per;$where=[];$params=[];if($status!=='all'){$where[]='a.status_bucket=?';$params[]=$status;}if($region!=='all'){$where[]='a.region=?';$params[]=$region;}if($country!=='all'){$where[]='a.country_code=?';$params[]=$country;}if($q!==''){$where[]='(a.name LIKE ? OR a.iata_code LIKE ? OR a.icao_code LIKE ? OR a.callsign LIKE ? OR a.hubs LIKE ? OR c.name LIKE ?)';for($i=0;$i<6;$i++)$params[]='%'.$q.'%';}$w=$where?'WHERE '.implode(' AND ',$where):'';$total=(int)scalar("SELECT COUNT(*) FROM airlines a LEFT JOIN countries c ON c.code=a.country_code $w",$params);$list=rows("SELECT a.*, c.name country_name FROM airlines a LEFT JOIN countries c ON c.code=a.country_code $w ORDER BY a.name LIMIT $per OFFSET $offset",$params);$regions=rows("SELECT DISTINCT region value, region label FROM airlines WHERE region IS NOT NULL AND region <> '' ORDER BY region"); ?>
<section class="view"><section class="section-head"><div><div class="eyebrow">Airline registry</div><h2>Airlines</h2></div><p>Search by name, IATA/ICAO code, callsign, country or region. Profile depth depends on user tier.</p></section><div class="filters panel"><form method="get"><input type="hidden" name="page" value="airlines"><input name="q" value="<?=e($q)?>" placeholder="Search airline, code, callsign…"><select name="status" data-auto-submit><option value="active" <?=$status==='active'?'selected':''?>>Active</option><option value="defunct" <?=$status==='defunct'?'selected':''?>>Defunct</option><option value="all" <?=$status==='all'?'selected':''?>>All statuses</option></select><select name="region" data-auto-submit><?=option_tags($regions,$region,'All regions')?></select><select name="country" data-auto-submit><?=option_tags(country_options(),$country,'All countries')?></select><button class="btn ink" type="submit">Search</button></form></div><p class="muted">Showing <?=nfmt(count($list))?> of <?=nfmt($total)?> records.</p><div class="card-grid"><?php foreach($list as $a): ?><a class="record-link" href="?page=airline&id=<?=e($a['id'])?>"><article class="record-card" style="--h:<?=hue($a['name'])?>"><div class="topline"><span class="chip gold"><?=e($a['country_name']?:$a['country_code'])?></span><?=status_chip($a['status_bucket'])?></div><h3><?=e($a['name'])?></h3><p><?=e($a['callsign']?:$a['hubs']?:'Airline record')?></p><div class="code-row"><?php if($a['iata_code']): ?><span class="chip">IATA <?=e($a['iata_code'])?></span><?php endif; ?><?php if($a['icao_code']): ?><span class="chip">ICAO <?=e($a['icao_code'])?></span><?php endif; ?><?php if($a['fleet_size']): ?><span class="chip">Fleet <?=e($a['fleet_size'])?></span><?php endif; ?></div></article></a><?php endforeach; ?></div><?=paginate($total,$per)?></section>
<?php elseif($page === 'airline'): $id=(int)getv('id'); $a=row('SELECT a.*, c.name country_name FROM airlines a LEFT JOIN countries c ON c.code=a.country_code WHERE a.id=?',[$id]); if(!$a): ?><section class="view"><div class="empty-state"><h2>Airline not found</h2></div></section><?php else: $fleet=rows('SELECT * FROM aircraft_registrations WHERE (operator_icao=? OR icao_code=? OR operator_name LIKE ?) ORDER BY aircraft_type, registration LIMIT 80',[$a['icao_code'],$a['icao_code'],'%'.$a['name'].'%']); $routes=rows('SELECT ars.*, o.iata_code origin_iata,d.iata_code dest_iata,o.city_name origin_city,d.city_name dest_city FROM airline_route_services ars JOIN route_markets rm ON rm.id=ars.route_market_id JOIN airports o ON o.id=rm.origin_airport_id JOIN airports d ON d.id=rm.destination_airport_id WHERE ars.airline_id=? ORDER BY o.iata_code,d.iata_code LIMIT 50',[$id]); $dests=rows('SELECT * FROM airline_destinations WHERE airline_iata=? OR airline_icao=? OR airline_name=? ORDER BY hub DESC,country,city LIMIT 80',[$a['iata_code'],$a['icao_code'],$a['name']]); ?><section class="view"><a class="btn ghost" href="?page=airlines">← Back to airlines</a><div class="profile-hero" style="--h:<?=hue($a['name'])?>"><div class="kicker"><?=e($a['country_name']?:$a['country_code'])?> · <?=e(ucfirst($a['status_bucket']))?></div><h1 class="profile-title"><?=e($a['name'])?></h1><p class="lead"><?=e(airline_summary($a))?></p><div class="data-ribbon"><?=status_chip($a['status_bucket'])?><?php if($a['iata_code']): ?><span class="chip dark">IATA <?=e($a['iata_code'])?></span><?php endif; ?><?php if($a['icao_code']): ?><span class="chip dark">ICAO <?=e($a['icao_code'])?></span><?php endif; ?></div></div><div class="profile-grid"><div class="panel"><div class="eyebrow">Airline identity</div><dl class="detail-list"><div><dt>Country</dt><dd><?=e($a['country_name']?:$a['country_code'])?></dd></div><div><dt>Fleet size field</dt><dd><?=fmt($a['fleet_size'])?></dd></div><div><dt>Destinations field</dt><dd><?=fmt($a['destinations_count'])?></dd></div><div><dt>Hubs</dt><dd><?=e($a['hubs']?:'—')?></dd></div><div><dt>Alliance</dt><dd><?=e($a['alliance']?:'—')?></dd></div><div><dt>Parent</dt><dd><?=e($a['parent_company']?:'—')?></dd></div><div><dt>Last modified</dt><dd><?=e($a['date_modified']?:'—')?></dd></div></dl></div><div class="panel dark"><div class="eyebrow">Linked intelligence</div><div class="stats-grid compact"><div class="stat-card darkstat"><strong><?=nfmt(count($fleet))?></strong><span>Aircraft</span></div><div class="stat-card darkstat"><strong><?=nfmt(count($routes)+count($dests))?></strong><span>Routes / destinations</span></div></div></div></div><section class="section-head"><div><div class="eyebrow">Routes and competitors</div><h2>Network</h2></div><p>Normalised route services use airports, schedules and equipment. Legacy destination rows are retained as fallback.</p></section><?php if($routes): ?><?=render_result_table($routes)?><?php else: ?><?=render_result_table($dests)?><?php endif; ?><section class="section-head"><div><div class="eyebrow">Fleet linkage</div><h2>Aircraft</h2></div><p>Aircraft matched by operator ICAO/name. Ownership history is reserved for Pro+ users.</p></section><?php if(feature_allowed('aircraft_history')): ?><?=render_result_table($fleet)?><?php else: ?><?=access_gate('Aircraft history is a Pro feature','The fleet list preview is available, but past owners, lessors, engines and cabin configuration require Pro access.','Upgrade to Pro')?><?php endif; ?></section><?php endif; ?>
<?php elseif($page === 'airports'): $q=getv('q');$country=getv('country','all');$where=[];$params=[];if($country!=='all'){$where[]='country_code=?';$params[]=$country;}if($q!==''){$where[]='(airport_name LIKE ? OR city_name LIKE ? OR iata_code LIKE ? OR icao_code LIKE ?)';for($i=0;$i<4;$i++)$params[]='%'.$q.'%';}$w=$where?'WHERE '.implode(' AND ',$where):'';$list=rows("SELECT ap.*, c.name country_name FROM airports ap LEFT JOIN countries c ON c.code=ap.country_code $w ORDER BY airport_name LIMIT 300",$params); ?><section class="view"><section class="section-head"><div><div class="eyebrow">Airport master data</div><h2>Airports</h2></div><p>Airport records include codes, city, geolocation, elevation and hub/base relationships.</p></section><div class="filters panel"><form method="get"><input type="hidden" name="page" value="airports"><input name="q" value="<?=e($q)?>" placeholder="Search airport, city, code…"><select name="country" data-auto-submit><?=option_tags(country_options(),$country,'All countries')?></select><button class="btn ink">Search</button></form></div><div class="card-grid"><?php foreach($list as $ap): ?><article class="record-card" style="--h:<?=hue($ap['airport_name']?:$ap['iata_code'])?>"><div class="topline"><span class="chip gold"><?=e($ap['country_name']?:$ap['country_code'])?></span><span class="chip"><?=e($ap['elevation_ft']?'Elev. '.$ap['elevation_ft'].' ft':'Airport')?></span></div><h3><?=e($ap['airport_name']?:'Unnamed airport')?></h3><p><?=e($ap['city_name']?:'—')?></p><div class="code-row"><?php if($ap['iata_code']): ?><span class="chip">IATA <?=e($ap['iata_code'])?></span><?php endif; ?><?php if($ap['icao_code']): ?><span class="chip">ICAO <?=e($ap['icao_code'])?></span><?php endif; ?></div></article><?php endforeach; ?></div></section>
<?php elseif($page === 'aircraft'): $q=getv('q');$country=getv('country','all');$per=60;$offset=(page_num()-1)*$per;$where=[];$params=[];if($country!=='all'){$where[]='country_code=?';$params[]=$country;}if($q!==''){$where[]='(registration LIKE ? OR aircraft_type LIKE ? OR icao_code LIKE ? OR operator_name LIKE ? OR operator_icao LIKE ?)';for($i=0;$i<5;$i++)$params[]='%'.$q.'%';}$w=$where?'WHERE '.implode(' AND ',$where):'';$total=(int)scalar("SELECT COUNT(*) FROM aircraft_registrations $w",$params);$list=rows("SELECT * FROM aircraft_registrations $w ORDER BY age DESC, registration LIMIT $per OFFSET $offset",$params); ?><section class="view"><section class="section-head"><div><div class="eyebrow">Aircraft register</div><h2>Aircraft</h2></div><p>Search registration, type, operator and country. The schema now supports configuration, ownership, engines and past operators.</p></section><div class="filters panel"><form method="get"><input type="hidden" name="page" value="aircraft"><input name="q" value="<?=e($q)?>" placeholder="Search registration, type, operator…"><select name="country" data-auto-submit><?=option_tags(country_options(),$country,'All countries')?></select><button class="btn ink">Search</button></form></div><p class="muted">Showing <?=nfmt(count($list))?> of <?=nfmt($total)?> aircraft records.</p><?=render_result_table($list)?><?=paginate($total,$per)?></section>
<?php elseif($page === 'routes'): if(!feature_allowed('route_intelligence')): echo access_gate('Route intelligence requires an Analyst account','Routes combine airports, schedules, equipment and competitors. This is one of the core paid product areas.','View tiers'); else: $routes=rows("SELECT ars.id, CONCAT(o.iata_code,'-',d.iata_code) route, a.name airline, ars.flight_number_prefix, ars.status, rsp.frequency_per_week, rsp.departure_time_local, rsp.arrival_time_local, GROUP_CONCAT(DISTINCT atp.icao_code SEPARATOR ', ') equipment FROM airline_route_services ars JOIN airlines a ON a.id=ars.airline_id JOIN route_markets rm ON rm.id=ars.route_market_id JOIN airports o ON o.id=rm.origin_airport_id JOIN airports d ON d.id=rm.destination_airport_id LEFT JOIN route_schedule_patterns rsp ON rsp.airline_route_service_id=ars.id LEFT JOIN route_service_equipment rse ON rse.airline_route_service_id=ars.id LEFT JOIN aircraft_types atp ON atp.id=rse.aircraft_type_id GROUP BY ars.id,o.iata_code,d.iata_code,a.name,ars.flight_number_prefix,ars.status,rsp.frequency_per_week,rsp.departure_time_local,rsp.arrival_time_local ORDER BY route, airline"); ?><section class="view"><section class="section-head"><div><div class="eyebrow">Route intelligence</div><h2>Routes, equipment and competitors</h2></div><p>The route model separates airport-pair markets from airline services, schedules, aircraft equipment and traffic statistics.</p></section><?=render_result_table($routes)?><section class="section-head"><div><div class="eyebrow">Competitive markets</div><h2>Routes with more than one operator</h2></div></section><?=chart_bars(run_insight_query('routes_with_competition'))?></section><?php endif; ?>
<?php elseif($page === 'regulatory'): if(!feature_allowed('regulatory_intelligence') && $user): echo access_gate('Regulatory intelligence requires Analyst+','AOC data, authorities, bilateral rights and regulatory documents are high-value datasets.','Upgrade'); else: $q=getv('q');$country=getv('country','all');$where=[];$params=[];if($country!=='all'){$where[]='r.country_code=?';$params[]=$country;}if($q!==''){$where[]='(r.name LIKE ? OR r.regulator_name LIKE ? OR r.airline_iata LIKE ? OR r.airline_icao LIKE ? OR r.aoc_number LIKE ? OR r.authority LIKE ? OR r.city LIKE ?)';for($i=0;$i<7;$i++)$params[]='%'.$q.'%';}$w=$where?'WHERE '.implode(' AND ',$where):'';$list=rows("SELECT r.*, c.name country_name FROM regulatory_records r LEFT JOIN countries c ON c.code=r.country_code $w ORDER BY r.country_code,r.type,r.name LIMIT 300",$params); ?><section class="view"><section class="section-head"><div><div class="eyebrow">Regulatory intelligence</div><h2>Regulatory</h2></div><p>Regulators, AOC records, civil aviation authorities and contact fields. The new schema adds AOCs, documents, bilateral agreements and traffic rights.</p></section><div class="filters panel"><form method="get"><input type="hidden" name="page" value="regulatory"><input name="q" value="<?=e($q)?>" placeholder="Search authority, AOC, airline…"><select name="country" data-auto-submit><?=option_tags(country_options(),$country,'All countries')?></select><button class="btn ink">Search</button></form></div><div class="card-grid"><?php foreach($list as $r): ?><article class="record-card" style="--h:<?=hue($r['name']?:$r['type'])?>"><div class="topline"><span class="chip gold"><?=e($r['type'])?></span><?=status_chip($r['status_bucket'])?></div><h3><?=e($r['name'])?></h3><p><?=e($r['country_name']?:$r['country_code'])?><?=$r['city']?' · '.e($r['city']):''?></p><div class="code-row"><?php if($r['aoc_number']): ?><span class="chip">AOC <?=e($r['aoc_number'])?></span><?php endif; ?><?php if($r['airline_icao']): ?><span class="chip">ICAO <?=e($r['airline_icao'])?></span><?php endif; ?></div></article><?php endforeach; ?></div></section><?php endif; ?>
<?php elseif($page === 'datasets'): $catalog=rows("SELECT category, COUNT(*) csv_files, SUM(is_populated) populated_files, SUM(row_count) rows_total, COUNT(DISTINCT CASE WHEN is_populated=1 THEN country_code END) countries FROM dataset_files GROUP BY category ORDER BY rows_total DESC, category"); ?><section class="view"><section class="section-head"><div><div class="eyebrow">Dataset catalogue</div><h2>Available and planned tables</h2></div><p>Populated source files remain visible alongside the new normalised schema so users can see both raw coverage and finished product areas.</p></section><div class="dataset-grid"><?php foreach($catalog as $c): $live=(int)$c['rows_total']>0; ?><article class="dataset-card"><div class="topline"><span class="chip <?=$live?'ok':'gold'?>"><?=$live?'Populated':'Coming soon'?></span><span class="chip"><?=nfmt($c['csv_files'])?> files</span></div><h3><?=e($c['category']?:'Root')?></h3><p><?=$live?'Contains populated rows and can be connected to interfaces.':'Template exists but needs data.'?></p><div class="progress"><i style="width:<?=min(100,max(4,((int)$c['populated_files']/max(1,(int)$c['csv_files']))*100))?>%"></i></div><div class="data-ribbon"><span class="chip"><?=nfmt($c['rows_total']?:0)?> rows</span><span class="chip"><?=nfmt($c['countries']?:0)?> countries</span></div></article><?php endforeach; ?></div></section>
<?php elseif($page === 'account'): if(!$user): echo access_gate('Log in required','Manage your account and subscription tier after logging in.'); else: ?><section class="auth-shell"><div class="panel auth-card"><div class="eyebrow">Account</div><h1><?=e($user['name'])?></h1><p><?=tier_badge($user)?> <?=e($user['email'])?></p><form method="post" class="stack-form"><?=csrf_field()?><input type="hidden" name="action" value="update_account"><label>Name<input name="name" value="<?=e($user['name'])?>" required></label><button class="btn ink">Save profile</button></form><div class="hero-actions"><a class="btn ghost" href="?page=pricing">Compare tiers</a><a class="btn ghost" href="?page=logout">Log out</a></div></div></section><?php endif; ?>
<?php elseif($page === 'admin'): if(!is_admin()): echo access_gate('Admin only','You need an administrator account to manage tiers, users, insights and dataset operations.'); else: $tab=getv('tab','tasks'); $tiers=tier_cards(); ?><section class="view admin-view"><section class="section-head"><div><div class="eyebrow">Admin control centre</div><h2>Build plan, access and homepage intelligence</h2></div><p>This area defines the work required to get Angani Data to 100%, controls rotating homepage charts, and manages user access tiers.</p></section><div class="tabs"><a class="<?=active_page($tab,'tasks')?>" href="?page=admin&tab=tasks">Tasks</a><a class="<?=active_page($tab,'insights')?>" href="?page=admin&tab=insights">Insights</a><a class="<?=active_page($tab,'users')?>" href="?page=admin&tab=users">Users</a><a class="<?=active_page($tab,'tiers')?>" href="?page=admin&tab=tiers">Tiers</a><a href="admin/pages/bulk_operations.php">Bulk ops file</a></div><?php if($tab==='tasks'): $tasks=['Audit existing CSV fields and map them into normalised entities','Complete auth, password reset and email verification','Add billing/payment integration and automatic tier upgrade/downgrade','Finish admin CRUD for airlines, airports, aircraft, routes and regulatory documents','Build import validators for each dataset family','Add source-record evidence viewer and change approval workflow','Create export permissions and usage limits per tier','Add saved searches, watchlists and alerts','Create API key management for Team/Enterprise tiers','Add route market pages with competitor and traffic charts','Add aircraft profile pages with registration, owners, engines and cabin history','Add airport profile pages with runways, operators, hubs and traffic stats','Add data-quality dashboard: stale records, conflicts and missing source links','Add background scraper jobs for Wikipedia, airline sites and CAA websites','Add deployment hardening: backups, cron jobs, logs, error reporting and admin audit trail']; ?><div class="task-board"><?php foreach($tasks as $i=>$t): ?><div class="task"><span><?=str_pad((string)($i+1),2,'0',STR_PAD_LEFT)?></span><p><?=e($t)?></p></div><?php endforeach; ?></div><?php elseif($tab==='insights'): $cards=rows('SELECT * FROM insight_cards ORDER BY display_order,id'); ?><div class="panel"><h3>Add / edit homepage insight</h3><form method="post" class="admin-form"><?=csrf_field()?><input type="hidden" name="action" value="admin_save_insight"><input name="id" placeholder="ID for edit"><input name="title" placeholder="Title" required><input name="metric_label" placeholder="Metric label"><select name="query_key"><option value="oldest_aircraft">Oldest aircraft</option><option value="highest_airports">Highest airports</option><option value="smallest_airlines_capacity">Smallest airlines by fleet</option><option value="routes_with_competition">Competitive routes</option><option value="dataset_coverage">Dataset coverage</option><option value="regulatory_depth">Regulatory depth</option></select><select name="chart_type"><option value="ranked_bar">Ranked bar</option><option value="metric">Metric</option><option value="list">List</option></select><select name="required_tier_id"><option value="">Public</option><?php foreach($tiers as $t): ?><option value="<?=e($t['id'])?>"><?=e($t['name'])?></option><?php endforeach; ?></select><input name="display_order" value="10"><select name="is_active"><option value="1">Active</option><option value="0">Hidden</option></select><textarea name="description" placeholder="Description"></textarea><button class="btn ink">Save insight</button></form></div><?=render_result_table($cards)?><?php elseif($tab==='users'): $users=rows('SELECT u.id,u.name,u.email,u.role,u.status,st.name tier_name,u.created_at FROM users u LEFT JOIN subscription_tiers st ON st.id=u.tier_id ORDER BY u.created_at DESC'); ?><div class="panel"><h3>Change user tier</h3><form method="post" class="admin-form compact"><?=csrf_field()?><input type="hidden" name="action" value="admin_update_user_tier"><input name="user_id" placeholder="User ID"><select name="tier_id"><?php foreach($tiers as $t): ?><option value="<?=e($t['id'])?>"><?=e($t['name'])?></option><?php endforeach; ?></select><button class="btn ink">Update</button></form></div><?=render_result_table($users)?><?php elseif($tab==='tiers'): ?><div class="pricing-grid"><?php foreach($tiers as $tier): ?><article class="pricing-card"><h3><?=e($tier['name'])?></h3><p><?=e($tier['description'])?></p><div class="price">$<?=e($tier['monthly_usd'])?></div><ul><?php foreach(tier_features((int)$tier['id']) as $f): ?><li><?=e($f['feature_label'])?></li><?php endforeach; ?></ul></article><?php endforeach; ?></div><?php endif; ?></section><?php endif; ?>
<?php elseif($page === 'contact'): ?><section class="view"><div class="contact-card"><div class="panel dark"><img src="assets/angani-logo-white.png" class="mini-logo" alt="Angani"><h1>Angani Data</h1><p>For aviation data licensing, enrichment, route analysis and custom dashboards.</p></div><div class="panel"><h2>Angani Solutions</h2><p><strong>Silas Savali</strong></p><p><a href="mailto:info@angani.co.uk">info@angani.co.uk</a><br><a href="https://www.angani.co.uk">www.angani.co.uk</a></p></div></div></section><?php endif; ?>
<?php endif; ?>
</main>
<footer class="site-footer"><div><img src="assets/angani-logo-black.png" alt="Angani" class="footer-logo"><p>© <span id="year"></span> Angani Solutions. Aviation reference intelligence for markets, routes, fleets, airports and regulatory research.</p></div><div class="footer-links"><a href="?page=datasets">Datasets</a><a href="?page=pricing">Pricing</a><a href="?page=contact">Contact</a><a href="https://www.angani.co.uk" target="_blank" rel="noreferrer">angani.co.uk</a></div></footer>
<button class="to-top" id="toTop" type="button" aria-label="Back to top">↑</button><script src="js/app.js"></script>
</body></html>
=======
<?php elseif($page==='home'): $insights=active_insight_cards(); ?>
<section class="view home-view"><div class="hero premium-hero"><div><div class="kicker">Angani Solutions · Aviation Data Product</div><h1>Static aviation intelligence arranged for decisions.</h1><p class="lead">A serious PHP/MySQL aviation data platform for countries, airlines, airports, infrastructure, aircraft types, lessors, commercial reference, IATA/IOSA, regulatory and route intelligence.</p><div class="hero-actions"><a class="btn primary" href="?page=register">Create account</a><a class="btn secondary" href="?page=catalogue">Browse databases</a><a class="btn secondary" href="?page=pricing">View access</a></div></div><aside class="hero-panel cockpit-panel"><div class="cockpit-line"><span>ACTIVE AIRLINES</span><b class="active-pill"><?=nfmt($stats['active_airlines'])?></b></div><h2>Data product, not just CSVs.</h2><p>Admin CRUD, import/export, user tiers, drill-down pages, reference codes and source/change controls are built into the platform.</p></aside></div>
<form class="global-search-panel" method="get"><input type="hidden" name="page" value="search"><label><span>Search aviation datasets</span><input name="q" placeholder="Try: Kenya Airways, Nairobi, A320, IOSA, NDB, fare rules..."></label><button class="btn ink">Search Angani Data</button></form>
<div class="stats-grid"><?=metric_card(nfmt($stats['airlines']),'Airline records',nfmt($stats['active_airlines']).' active')?><?=metric_card(nfmt($stats['airports']),'Airport records','Infrastructure-ready')?><?=metric_card(nfmt($stats['aircraft_types']),'Aircraft types','Specs, engines, payload')?><?=metric_card(nfmt($stats['navaids']),'Navaids','Infrastructure & AIM')?></div>
<section class="section-head"><div><div class="eyebrow">Rotating homepage intelligence</div><h2>Interesting titbits that encourage signups</h2></div><p>These cards are controlled from Admin → Homepage Insights and can be rotated without editing code.</p></section><div class="insight-grid"><?php foreach($insights as $card): $rows=run_insight_query($card['query_key']); $locked=!empty($card['required_tier_id']) && !is_logged_in(); ?><article class="insight-card <?=$locked?'locked':''?>"><div class="topline"><span class="chip gold"><?=e($card['metric_label'])?></span><?php if($locked): ?><span class="chip danger">Pro preview</span><?php endif; ?></div><h3><?=e($card['title'])?></h3><p><?=e($card['description'])?></p><?=$locked?access_gate('Sign in to unlock the detail','This card previews a paid intelligence query.','See plans'):chart_bars($rows)?></article><?php endforeach; ?></div>
<section class="section-head"><div><div class="eyebrow">Product modules</div><h2>Designed around aviation workflows</h2></div><p>Each module supports search, filtering, drill-downs, admin CRUD and CSV import/export.</p></section><div class="module-grid"><?php foreach(module_groups() as $group=>$keys): ?><article class="module-group"><h3><?=e($group)?></h3><div><?php foreach($keys as $k): $c=module_config($k); if(!$c) continue; ?><a class="module-chip" href="<?=e(module_url($k))?>"><b><?=e($c['icon']??'DB')?></b><span><?=e($c['label'])?></span><em><?=nfmt(module_count($k))?></em></a><?php endforeach; ?></div></article><?php endforeach; ?></div></section>
<?php elseif($page==='search'): $q=getv('q'); $results=$q!==''?global_search($q):[]; ?>
<section class="view"><section class="section-head"><div><div class="eyebrow">Global search</div><h1>Search across Angani Data</h1></div><p>Search airlines, airports, aircraft types, reference tables, infrastructure, regulatory and commercial records from one place. Pro-only records show access gates when opened.</p></section><form class="global-search-panel standalone" method="get"><input type="hidden" name="page" value="search"><label><span>Search term</span><input name="q" value="<?=e($q)?>" placeholder="Airline, airport, aircraft type, code, country, route, reference..."></label><button class="btn ink">Search</button><a class="btn ghost" href="?page=catalogue">Browse modules</a></form><?php if($q===''): ?><div class="search-suggestions"><h3>Popular searches</h3><a href="?page=search&q=A320">A320</a><a href="?page=search&q=Nairobi">Nairobi</a><a href="?page=search&q=Kenya">Kenya</a><a href="?page=search&q=IOSA">IOSA</a><a href="?page=search&q=Tower">Tower frequencies</a><a href="?page=search&q=Booking">Booking classes</a></div><?php else: ?><div class="result-summary"><strong><?=nfmt(count($results))?></strong> results for <b><?=e($q)?></b></div><?=render_global_search_results($results)?><?php endif; ?></section>
<?php elseif($page==='catalogue'): ?>
<section class="view"><section class="section-head"><div><div class="eyebrow">Database catalogue</div><h1>Angani Data modules</h1></div><p>Free users see public/reference data. Pro users unlock full drill-downs and exports. Enterprise is for bulk/API access.</p></section><div class="module-grid"><?php foreach(module_groups() as $group=>$keys): ?><article class="module-group"><h3><?=e($group)?></h3><div><?php foreach($keys as $k): $c=module_config($k); if(!$c) continue; ?><a class="module-chip" href="<?=e(module_url($k))?>"><b><?=e($c['icon']??'DB')?></b><span><?=e($c['label'])?></span><em><?=nfmt(module_count($k))?></em></a><?php endforeach; ?></div></article><?php endforeach; ?></div></section>
<?php elseif($page==='module'): $key=getv('module','airlines'); $cfg=module_config($key); ?>
<section class="view"><?php if(!$cfg): ?><div class="empty-state"><h2>Unknown database</h2></div><?php elseif(!module_allowed($cfg)): ?><?=access_gate($cfg['label'].' is a Pro dataset','Create or upgrade your account to access this database.','View pricing')?><?php else: [$records,$total]=query_module_records($cfg,24,(page_num()-1)*24); ?><section class="section-head"><div><div class="eyebrow"><?=e($cfg['tier']==='free'?'Open database':'Pro database')?></div><h1><?=e($cfg['label'])?></h1></div><p><?=nfmt($total)?> records. Admin can add/edit/import; logged-in Pro users can export filtered data.</p></section><?=render_search_bar($key,$cfg)?><?php if(in_array($cfg['card']??'', ['airline','airport','aircraft_type','lessor','country','aircraft'], true)): ?><?=render_module_cards($key,$records)?><?php else: ?><?=render_table($records,$cfg['list'],$key)?><?php endif; ?><?=paginate($total,24)?><?php endif; ?></section>
<?php elseif($page==='detail'): $key=getv('module'); $cfg=module_config($key); $id=(int)getv('id'); ?>
<section class="view"><?php if(!$cfg || !$id): ?><div class="empty-state"><h2>Record not found</h2></div><?php elseif(!module_allowed($cfg)): ?><?=access_gate('Pro detail locked','Upgrade to open full records in this database.','View pricing')?><?php else: $r=row('SELECT * FROM `'.$cfg['table'].'` WHERE id=?',[$id]); if(!$r): ?><div class="empty-state"><h2>Record not found</h2></div><?php else: ?><a class="linkish" href="<?=e(module_url($key))?>">← Back to <?=e($cfg['label'])?></a><section class="record-hero"><div><div class="eyebrow"><?=e($cfg['label'])?></div><h1><?=e($r[$cfg['title']] ?? 'Record')?></h1><p><?=e($r[$cfg['subtitle']] ?? '')?></p></div><?php if(is_admin()): ?><a class="btn ink" href="?page=admin&tab=edit&module=<?=e($key)?>&id=<?=(int)$id?>">Edit in Admin</a><?php endif; ?></section><?=render_detail_fields($cfg,$r,false)?><?=render_related_sections($key,$r)?><?php endif; endif; ?></section>
<?php elseif($page==='pricing'): $tiers=tier_cards(); ?>
<section class="view"><section class="section-head"><div><div class="eyebrow">Simple access model</div><h1>Three tiers only</h1></div><p>Free for discovery, Pro for paid intelligence, Enterprise for custom/API/bulk work.</p></section><div class="pricing-grid"><?php foreach($tiers as $tier): ?><article class="pricing-card <?=$tier['code']==='pro'?'featured':''?>"><div class="topline"><span class="chip gold"><?=e($tier['name'])?></span><span class="chip"><?= $tier['code']==='enterprise'?'Contact us':'$'.e($tier['monthly_usd']).'/mo' ?></span></div><h3><?=e($tier['name'])?></h3><p><?=e($tier['description'])?></p><div class="price"><?= $tier['code']==='enterprise'?'Custom':'$'.e($tier['monthly_usd']).'<small>/month</small>' ?></div><ul><?php foreach(tier_features((int)$tier['id']) as $f): ?><li><?=e($f['feature_label'])?></li><?php endforeach; ?></ul><a class="btn <?=$tier['code']==='pro'?'primary':'ghost'?>" href="<?=$tier['code']==='enterprise'?'?page=contact':'?page=register'?>"><?=$tier['code']==='enterprise'?'Contact us':'Start '.$tier['name']?></a></article><?php endforeach; ?></div></section>
<?php elseif($page==='login'): ?>
<section class="auth-shell"><div class="panel auth-card"><div class="eyebrow">Member access</div><h1>Log in</h1><p class="muted">Demo admin: <code>admin@angani.co.uk</code> / <code>Angani@2026</code></p><form method="post" class="stack-form"><?=csrf_field()?><input type="hidden" name="action" value="login"><label>Email<input name="email" type="email" required></label><label>Password<input name="password" type="password" required></label><button class="btn ink" type="submit">Log in</button></form><p>New user? <a class="linkish" href="?page=register">Create account</a></p></div></section>
<?php elseif($page==='register'): ?>
<section class="auth-shell"><div class="panel auth-card"><div class="eyebrow">Create account</div><h1>Start exploring</h1><form method="post" class="stack-form"><?=csrf_field()?><input type="hidden" name="action" value="register"><label>Name<input name="name" required></label><label>Email<input name="email" type="email" required></label><label>Password<input name="password" type="password" minlength="8" required></label><button class="btn ink" type="submit">Create account</button></form></div></section>
<?php elseif($page==='account'): if(!$user) redirect_to('?page=login'); ?>
<section class="view"><section class="section-head"><div><div class="eyebrow">Account</div><h1><?=e($user['name'])?></h1></div><?=tier_badge($user)?></section><div class="panel-grid"><div class="panel"><h3>Profile</h3><form method="post" class="stack-form"><?=csrf_field()?><input type="hidden" name="action" value="update_account"><label>Name<input name="name" value="<?=e($user['name'])?>"></label><label>Email<input value="<?=e($user['email'])?>" disabled></label><button class="btn ink">Save</button></form></div><div class="panel"><h3>Access</h3><p>Your current tier is <strong><?=e($user['tier_name'])?></strong>.</p><p>CSV exports and full drilldowns are available on Pro and Enterprise.</p><a class="btn primary" href="?page=pricing">Compare plans</a></div></div></section>
<?php elseif($page==='dashboard'): if(!$user) redirect_to('?page=login'); $presets=question_presets(); $dashInsights=active_insight_cards(); ?>
<section class="view"><section class="dashboard-hero panel dark"><div><div class="eyebrow">Member cockpit</div><h1>Welcome, <?=e($user['name'])?></h1><p>Use preset questions, global search and database drill-downs to turn the aviation datasets into answers.</p></div><div class="stats-grid compact"><?=metric_card(nfmt($stats['airlines']),'Airlines',nfmt($stats['active_airlines']).' active')?><?=metric_card(nfmt($stats['airports']),'Airports','global coverage')?><?=metric_card(nfmt($stats['aircraft_types']),'Aircraft types','technical intelligence')?><?=metric_card(nfmt($stats['dataset_files']),'Dataset files','source warehouse')?></div></section><form class="global-search-panel standalone" method="get"><input type="hidden" name="page" value="search"><label><span>Search your aviation data</span><input name="q" placeholder="Search across airlines, airports, aircraft types, routes, standards..."></label><button class="btn ink">Search</button><a class="btn ghost" href="?page=catalogue">Browse catalogue</a></form><section class="section-head"><div><div class="eyebrow">Preset intelligence questions</div><h1>Ask the dataset</h1></div><p>These are controlled from Admin → Preset Questions and are designed to guide logged-in users toward useful answers.</p></section><div class="question-grid"><?php foreach($presets as $q): $locked=!empty($q['required_tier_order']) && tier_order_for()< (int)$q['required_tier_order'] && !is_admin(); ?><article class="question-card"><span class="chip"><?=e($q['category'])?></span><h3><?=e($q['title'])?></h3><p><?=e($q['question_text'])?></p><?php if($locked): ?><a class="btn ghost" href="?page=pricing">Upgrade to <?=e($q['required_tier_name'])?></a><?php else: ?><a class="btn primary" href="?page=answer&q=<?=e($q['answer_key'])?>">Run question</a><?php endif; ?></article><?php endforeach; ?></div><section class="section-head"><div><div class="eyebrow">Live titbits</div><h2>Charts from current data</h2></div><p>The same insight engine powers public teasers and logged-in analysis cards.</p></section><div class="insight-grid"><?php foreach(array_slice($dashInsights,0,6) as $card): $rows=run_insight_query($card['query_key']); ?><article class="insight-card"><div class="topline"><span class="chip gold"><?=e($card['metric_label'])?></span></div><h3><?=e($card['title'])?></h3><p><?=e($card['description'])?></p><?=chart_bars($rows)?></article><?php endforeach; ?></div></section>
<?php elseif($page==='answer'): if(!$user) redirect_to('?page=login'); $answerKey=getv('q'); $preset=preset_for_answer_key($answerKey); if($preset && !preset_allowed($preset)): ?><?=access_gate('Pro question locked','Upgrade to run this preset intelligence question.','View pricing')?><?php else: $rows=run_preset_query($answerKey); ?>
<section class="view"><a class="linkish" href="?page=dashboard">← Back to questions</a><section class="section-head"><div><div class="eyebrow">Preset answer</div><h1><?=e($preset['title'] ?? labelize($answerKey))?></h1></div><a class="btn ghost" href="?page=catalogue">Open datasets</a></section><?=render_answer_visual($answerKey,$rows)?><?=render_table($rows,$rows?array_keys($rows[0]):[],null)?></section><?php endif; ?>
<?php elseif($page==='admin'): if(!is_admin()): ?><?=access_gate('Admin access required','Log in with an administrator account to manage records, imports, exports and users.','Log in')?><?php else: render_admin_page(); endif; ?>
<?php elseif($page==='contact'): ?>
<section class="view"><div class="panel contact-panel"><div class="eyebrow">Enterprise package</div><h1>Contact Angani Solutions</h1><p>For bulk exports, API access, private dashboards, custom aviation datasets and advisory work.</p><p><strong>Silas Savali</strong><br>Angani Solutions<br><a href="mailto:info@angani.co.uk">info@angani.co.uk</a><br><a href="https://www.angani.co.uk">www.angani.co.uk</a></p></div></section>
<?php endif; ?></main><footer class="site-footer"><span>Angani Data</span><span>Built for aviation professionals, consultants, airports, airlines and regulators.</span></footer><script src="js/app.js"></script></body></html>
<?php
function admin_metric(string $label, $value, string $note='', string $tone=''): string {
    return '<article class="admin-kpi '.$tone.'"><span>'.e($label).'</span><strong>'.e(nfmt($value)).'</strong>'.($note?'<small>'.e($note).'</small>':'').'</article>';
}
function admin_safe_count(string $sql, array $params=[]): int { try{return (int)scalar($sql,$params);}catch(Throwable $e){return 0;} }
function admin_empty_modules(): array { $out=[]; foreach(modules() as $k=>$c){ if(module_count($k)===0) $out[$k]=$c; } return $out; }
function admin_module_nav(string $current='airlines'): string {
    $html='<div class="admin-module-nav">';
    foreach(module_groups() as $group=>$keys){
        $open=in_array($current,$keys,true)?' open':'';
        $html.='<details'.$open.'><summary>'.e($group).'</summary>';
        foreach($keys as $k){ $c=module_config($k); if(!$c) continue; $html.='<a class="'.($k===$current?'active':'').'" href="?page=admin&tab=records&module='.e($k).'">'.e($c['label']).' <small>'.nfmt(module_count($k)).'</small></a>'; }
        $html.='</details>';
    }
    return $html.'</div>';
}
function admin_search_bar(string $module, array $cfg): string {
    return '<form method="get" class="toolbar admin-toolbar"><input type="hidden" name="page" value="admin"><input type="hidden" name="tab" value="records"><input type="hidden" name="module" value="'.e($module).'"><label class="searchbox"><span>Search records</span><input name="q" value="'.e(getv('q')).'" placeholder="Search '.e($cfg['label']).'"></label><label class="searchbox small"><span>Country</span><input name="country" value="'.e(getv('country')).'" placeholder="KE"></label><button class="btn ink">Apply filters</button><a class="btn ghost" href="?page=admin&tab=records&module='.e($module).'">Reset</a><a class="btn ghost" href="?page=export&module='.e($module).'&'.e(http_build_query(['q'=>getv('q'),'country'=>getv('country')])).'">Export filtered CSV</a></form>';
}
function render_admin_page(): void {
    $tab=getv('tab','overview'); $module=getv('module','airlines'); $cfg=module_config($module);
    $nav=[
        'overview'=>'Command Center',
        'records'=>'Records & CRUD',
        'imports'=>'Imports / Exports',
        'quality'=>'Data Quality',
        'users'=>'Users',
        'plans'=>'Plans & Access',
        'questions'=>'Preset Questions',
        'insights'=>'Homepage Insights',
        'tasks'=>'100% Tasks'
    ];
    echo '<section class="admin-layout"><aside class="admin-side"><div class="admin-logo"><span>AD</span><div><h2>Admin Console</h2><p>Operational backend</p></div></div><nav class="admin-side-nav">';
    foreach($nav as $key=>$label){ echo '<a class="'.($tab===$key?'active':'').'" href="?page=admin&tab='.e($key).($key==='records'?'&module=airlines':'').'">'.e($label).'</a>'; }
    echo '</nav><div class="admin-side-foot"><small>Public frontend is separate from this console. Admin screens expose actions; public screens expose aviation intelligence.</small></div></aside><section class="admin-main">';
    if($tab==='overview') render_admin_overview();
    elseif($tab==='users') render_admin_users();
    elseif($tab==='plans') render_admin_plans();
    elseif($tab==='questions') render_admin_questions();
    elseif($tab==='insights') render_admin_insights();
    elseif($tab==='imports') render_admin_imports();
    elseif($tab==='quality') render_admin_quality();
    elseif($tab==='tasks') render_admin_tasks();
    elseif($tab==='edit') render_admin_edit($module,(int)getv('id'));
    else render_admin_records($module,$cfg);
    echo '</section></section>';
}
function render_admin_overview(): void {
    $s=get_stats(); $needs=admin_safe_count("SELECT COUNT(*) FROM staging_import_records WHERE status IN ('pending','needs_review','conflict')"); $bad=admin_safe_count("SELECT COUNT(*) FROM import_batches WHERE status IN ('failed','needs_review')"); $exports=admin_safe_count('SELECT COUNT(*) FROM export_logs'); $done=admin_safe_count("SELECT COUNT(*) FROM admin_tasks WHERE status='done'");
    echo '<div class="admin-head"><div><div class="eyebrow">Operations</div><h1>Admin Command Center</h1><p>Manage the data product, users, imports, quality review and homepage intelligence from one backend.</p></div><div class="hero-actions"><a class="btn primary" href="?page=admin&tab=records&module=airlines">Manage Records</a><a class="btn ghost" href="?page=admin&tab=imports">Import / Export</a></div></div>';
    echo '<div class="admin-kpi-grid">'.admin_metric('Airlines',$s['airlines'],nfmt($s['active_airlines']).' active','success').admin_metric('Airports',$s['airports'],'Infrastructure ready').admin_metric('Aircraft Types',$s['aircraft_types'],'Specs & type data').admin_metric('Users',$s['users'],'Accounts').admin_metric('Staging Review',$needs,'Rows needing attention',$needs?'warning':'success').admin_metric('Import Issues',$bad,'Batches to inspect',$bad?'danger':'success').admin_metric('Exports',$exports,'Logged downloads').admin_metric('Tasks Done',$done,'Readiness checklist').'</div>';
    echo '<div class="admin-action-grid"><article class="admin-action-card"><h3>Records</h3><p>Add, edit, preview and delete records across every configured aviation database.</p><a class="btn ink" href="?page=admin&tab=records&module=airlines">Open CRUD</a></article><article class="admin-action-card"><h3>Imports & Exports</h3><p>Append/truncate-import CSVs, export filtered module records, or export the whole database as CSV ZIP.</p><a class="btn ink" href="?page=admin&tab=imports">Open data ops</a></article><article class="admin-action-card"><h3>Data Quality</h3><p>Review staging rows, import batches, export logs and empty modules before launch.</p><a class="btn ink" href="?page=admin&tab=quality">Review quality</a></article><article class="admin-action-card"><h3>Users & Plans</h3><p>Manage users, roles, statuses and the simplified Free / Pro / Enterprise access model.</p><a class="btn ink" href="?page=admin&tab=users">Manage users</a></article></div>';
    echo '<section class="panel"><div class="topline"><h3>Database modules</h3><a class="btn mini ghost" href="?page=admin&tab=quality">View empty modules</a></div><div class="module-grid compact">';
    foreach(module_groups() as $group=>$keys){ echo '<article class="module-group"><h3>'.e($group).'</h3><div>'; foreach($keys as $k){ $c=module_config($k); if($c) echo '<a class="module-chip" href="?page=admin&tab=records&module='.e($k).'"><b>'.e($c['icon']??'DB').'</b><span>'.e($c['label']).'</span><em>'.nfmt(module_count($k)).'</em></a>'; } echo '</div></article>'; }
    echo '</div></section>';
}
function render_admin_records(string $module, ?array $cfg): void {
    if(!$cfg){echo '<div class="empty-state"><h2>Unknown module</h2></div>';return;}
    [$records,$total]=query_module_records($cfg,30,(page_num()-1)*30);
    echo '<div class="admin-head"><div><div class="eyebrow">Records & CRUD</div><h1>'.e($cfg['label']).'</h1><p>'.nfmt($total).' records · table <code>'.e($cfg['table']).'</code></p></div><div class="hero-actions"><a class="btn primary" href="?page=admin&tab=edit&module='.e($module).'">Add record</a><a class="btn ghost" href="?page=admin&tab=imports&module='.e($module).'">Import CSV</a><a class="btn ghost" href="?page=export&module='.e($module).'&'.e(http_build_query(['q'=>getv('q'),'country'=>getv('country')])).'">Export CSV</a></div></div>';
    echo admin_module_nav($module).admin_search_bar($module,$cfg).'<div class="admin-note"><strong>Admin mode:</strong> record actions are explicit here. Public users see clean database pages with internal IDs/source/import fields hidden.</div>'.render_admin_record_table($records,$cfg['list'],$module).paginate($total,30);
}
function render_admin_edit(string $module,int $id=0): void {
    $cfg=module_config($module); if(!$cfg){echo '<div class="empty-state"><h2>Unknown module</h2></div>';return;} $r=$id?row('SELECT * FROM `'.$cfg['table'].'` WHERE id=?',[$id]):[];
    echo '<div class="admin-head"><div><div class="eyebrow">'.($id?'Edit record':'Add record').'</div><h1>'.e($cfg['label']).'</h1><p>Only the editable business fields are shown. Internal IDs and import/source fields remain system-managed.</p></div><div class="hero-actions"><a class="btn ghost" href="?page=admin&tab=records&module='.e($module).'">Back to records</a>'.($id?'<a class="btn ghost" href="'.e(detail_url($module,$id)).'">Preview public detail</a>':'').'</div></div>';
    echo '<form method="post" class="admin-form">'.csrf_field().'<input type="hidden" name="action" value="admin_save_record"><input type="hidden" name="module" value="'.e($module).'"><input type="hidden" name="id" value="'.(int)$id.'">';
    foreach($cfg['fields'] as $f){ $val=$r[$f] ?? ''; $isText=str_contains($f,'description')||str_contains($f,'notes')||str_contains($f,'story')||str_contains($f,'text')||str_contains($f,'summary')||str_contains($f,'requirements')||str_contains($f,'url')||str_contains($f,'source')||str_contains($f,'history')||str_contains($f,'milestones')||str_contains($f,'json'); echo '<label><span>'.e(labelize($f)).'</span>'; if($isText) echo '<textarea name="'.e($f).'" rows="4">'.e($val).'</textarea>'; else echo '<input name="'.e($f).'" value="'.e($val).'">'; echo '</label>'; }
    echo '<div class="form-actions"><button class="btn ink">Save record</button></div></form>';
    if($id){ echo '<details class="admin-danger-zone"><summary>Danger zone</summary><form method="post" onsubmit="return confirm(\'Delete this record permanently?\')">'.csrf_field().'<input type="hidden" name="action" value="admin_delete_record"><input type="hidden" name="module" value="'.e($module).'"><input type="hidden" name="id" value="'.(int)$id.'"><button class="btn danger">Delete record</button></form></details>'; }
}
function render_admin_users(): void {
    $tiers=tier_cards(); $q=getv('q'); $params=[]; $where=''; if($q!==''){ $where='WHERE u.name LIKE ? OR u.email LIKE ? OR u.role LIKE ? OR u.status LIKE ?'; $params=['%'.$q.'%','%'.$q.'%','%'.$q.'%','%'.$q.'%']; }
    $users=rows('SELECT u.*, st.name tier_name FROM users u LEFT JOIN subscription_tiers st ON st.id=u.tier_id '.$where.' ORDER BY u.created_at DESC LIMIT 200',$params);
    echo '<div class="admin-head"><div><div class="eyebrow">Users</div><h1>Manage users</h1><p>Create accounts, assign Free/Pro/Enterprise, suspend access, and promote admins.</p></div></div>';
    echo '<div class="panel"><h3>Create user</h3><form method="post" class="admin-inline-form">'.csrf_field().'<input type="hidden" name="action" value="admin_save_user"><input name="name" placeholder="Name" required><input name="email" type="email" placeholder="Email" required><input name="password" placeholder="Password" value="Angani@2026"><select name="tier_id">'; foreach($tiers as $t) echo '<option value="'.(int)$t['id'].'">'.e($t['name']).'</option>'; echo '</select><select name="role"><option>user</option><option>admin</option></select><select name="status"><option>active</option><option>suspended</option></select><button class="btn ink">Create</button></form></div>';
    echo '<form method="get" class="toolbar admin-toolbar"><input type="hidden" name="page" value="admin"><input type="hidden" name="tab" value="users"><label class="searchbox"><span>Search users</span><input name="q" value="'.e($q).'" placeholder="Name, email, role, status"></label><button class="btn ink">Search</button><a class="btn ghost" href="?page=admin&tab=users">Reset</a></form>';
    echo '<div class="table-wrap"><table><thead><tr><th>Name</th><th>Email</th><th>Tier</th><th>Role</th><th>Status</th><th>Last login</th><th>Action</th></tr></thead><tbody>'; foreach($users as $u){ echo '<tr><form method="post">'.csrf_field().'<input type="hidden" name="action" value="admin_save_user"><input type="hidden" name="user_id" value="'.(int)$u['id'].'"><td><input name="name" value="'.e($u['name']).'"></td><td><input name="email" value="'.e($u['email']).'"></td><td><select name="tier_id">'; foreach($tiers as $t) echo '<option value="'.(int)$t['id'].'" '.((int)$t['id']===(int)$u['tier_id']?'selected':'').'>'.e($t['name']).'</option>'; echo '</select></td><td><select name="role"><option '.($u['role']==='user'?'selected':'').'>user</option><option '.($u['role']==='admin'?'selected':'').'>admin</option></select></td><td><select name="status"><option '.($u['status']==='active'?'selected':'').'>active</option><option '.($u['status']==='suspended'?'selected':'').'>suspended</option><option '.($u['status']==='deleted'?'selected':'').'>deleted</option></select></td><td>'.e($u['last_login_at'] ?? '—').'</td><td><button class="btn mini">Save</button></td></form></tr>'; } echo '</tbody></table></div>';
}
function render_admin_plans(): void {
    $tiers=rows('SELECT * FROM subscription_tiers ORDER BY display_order');
    echo '<div class="admin-head"><div><div class="eyebrow">Plans & Access</div><h1>Free / Pro / Enterprise</h1><p>Edit pricing, limits and visible benefits. Keep the public offer simple.</p></div></div><div class="pricing-grid admin-plan-grid">';
    foreach($tiers as $t){ echo '<article class="pricing-card"><form method="post" class="stack-form">'.csrf_field().'<input type="hidden" name="action" value="admin_save_tier"><input type="hidden" name="tier_id" value="'.(int)$t['id'].'"><label><span>Name</span><input name="name" value="'.e($t['name']).'"></label><label><span>Code</span><input name="code" value="'.e($t['code']).'"></label><label><span>Description</span><textarea name="description">'.e($t['description']).'</textarea></label><label><span>Monthly USD</span><input name="monthly_usd" value="'.e($t['monthly_usd']).'"></label><label><span>Annual USD</span><input name="annual_usd" value="'.e($t['annual_usd']).'"></label><label><span>Monthly exports</span><input name="export_limit_monthly" value="'.e($t['export_limit_monthly']).'"></label><label><span>API limit</span><input name="api_limit_monthly" value="'.e($t['api_limit_monthly']).'"></label><label><span>Order</span><input name="display_order" value="'.(int)$t['display_order'].'"></label><label><span>Active</span><select name="is_active"><option value="1" '.($t['is_active']?'selected':'').'>Yes</option><option value="0" '.(!$t['is_active']?'selected':'').'>No</option></select></label><button class="btn ink">Save plan</button></form><hr><h4>Benefits</h4><ul class="feature-list">'; foreach(rows('SELECT * FROM tier_features WHERE tier_id=? ORDER BY id',[(int)$t['id']]) as $f){ echo '<li>'.e($f['feature_label']).'<form method="post" class="inline-delete">'.csrf_field().'<input type="hidden" name="action" value="admin_delete_feature"><input type="hidden" name="feature_id" value="'.(int)$f['id'].'"><button class="btn mini danger">×</button></form></li>'; } echo '</ul><form method="post" class="inline-form">'.csrf_field().'<input type="hidden" name="action" value="admin_save_feature"><input type="hidden" name="tier_id" value="'.(int)$t['id'].'"><input name="feature_code" placeholder="feature_code" required><input name="feature_label" placeholder="Feature label" required><button class="btn mini">Add</button></form></article>'; }
    echo '</div>';
}
function render_admin_questions(): void {
    $qs=rows('SELECT qp.*, st.name tier_name FROM question_presets qp LEFT JOIN subscription_tiers st ON st.id=qp.required_tier_id ORDER BY qp.display_order, qp.id'); $tiers=tier_cards();
    echo '<div class="admin-head"><div><div class="eyebrow">Preset Questions</div><h1>User intelligence prompts</h1><p>These drive the logged-in dashboard. Use them to guide users toward valuable dataset answers.</p></div></div>';
    echo '<div class="panel"><h3>Add question</h3><form method="post" class="admin-form compact-form">'.csrf_field().'<input type="hidden" name="action" value="admin_save_question"><label><span>Code</span><input name="code" required></label><label><span>Title</span><input name="title" required></label><label><span>Category</span><input name="category" required></label><label><span>Answer key</span><input name="answer_key" required></label><label><span>Required tier</span><select name="required_tier_id"><option value="">Free</option>'; foreach($tiers as $t) echo '<option value="'.(int)$t['id'].'">'.e($t['name']).'</option>'; echo '</select></label><label><span>Order</span><input name="display_order" value="50"></label><label class="wide"><span>Question text</span><textarea name="question_text" required></textarea></label><label><span>Active</span><select name="is_active"><option value="1">Yes</option><option value="0">No</option></select></label><button class="btn ink">Add question</button></form></div>';
    echo '<div class="table-wrap"><table><thead><tr><th>Question</th><th>Category</th><th>Answer key</th><th>Tier</th><th>Order</th><th>Active</th><th>Save</th></tr></thead><tbody>'; foreach($qs as $q){ echo '<tr><form method="post">'.csrf_field().'<input type="hidden" name="action" value="admin_save_question"><input type="hidden" name="id" value="'.(int)$q['id'].'"><td><input name="code" value="'.e($q['code']).'"><input name="title" value="'.e($q['title']).'"><textarea name="question_text">'.e($q['question_text']).'</textarea></td><td><input name="category" value="'.e($q['category']).'"></td><td><input name="answer_key" value="'.e($q['answer_key']).'"></td><td><select name="required_tier_id"><option value="">Free</option>'; foreach($tiers as $t) echo '<option value="'.(int)$t['id'].'" '.((int)$q['required_tier_id']===(int)$t['id']?'selected':'').'>'.e($t['name']).'</option>'; echo '</select></td><td><input name="display_order" value="'.(int)$q['display_order'].'"></td><td><select name="is_active"><option value="1" '.($q['is_active']?'selected':'').'>Yes</option><option value="0" '.(!$q['is_active']?'selected':'').'>No</option></select></td><td><button class="btn mini">Save</button></td></form></tr>'; } echo '</tbody></table></div>';
}
function render_admin_insights(): void {
    $cards=rows('SELECT ic.*, st.name tier_name FROM insight_cards ic LEFT JOIN subscription_tiers st ON st.id=ic.required_tier_id ORDER BY ic.display_order'); $tiers=tier_cards(); $queries=available_insight_queries();
    echo '<div class="admin-head"><div><div class="eyebrow">Homepage</div><h1>Manage rotating insights</h1><p>Use these cards to keep the public homepage fresh and convert visitors into signups.</p></div></div>';
    echo '<div class="panel"><h3>Add homepage insight</h3><form method="post" class="admin-form compact-form">'.csrf_field().'<input type="hidden" name="action" value="admin_save_insight"><label><span>Title</span><input name="title" required></label><label><span>Metric label</span><input name="metric_label"></label><label><span>Query</span><select name="query_key">'; foreach($queries as $k=>$label) echo '<option value="'.e($k).'">'.e($label).'</option>'; echo '</select></label><label><span>Chart</span><input name="chart_type" value="ranked_bar"></label><label><span>Required tier</span><select name="required_tier_id"><option value="">Public</option>'; foreach($tiers as $t) echo '<option value="'.(int)$t['id'].'">'.e($t['name']).'</option>'; echo '</select></label><label><span>Order</span><input name="display_order" value="50"></label><label><span>Active</span><select name="is_active"><option value="1">Yes</option><option value="0">No</option></select></label><label class="wide"><span>Description</span><textarea name="description"></textarea></label><button class="btn ink">Add insight</button></form></div>';
    echo '<div class="table-wrap"><table><thead><tr><th>Title</th><th>Metric</th><th>Query</th><th>Tier</th><th>Order</th><th>Active</th><th>Save</th></tr></thead><tbody>'; foreach($cards as $c){ echo '<tr><form method="post">'.csrf_field().'<input type="hidden" name="action" value="admin_save_insight"><input type="hidden" name="id" value="'.(int)$c['id'].'"><td><input name="title" value="'.e($c['title']).'"><textarea name="description">'.e($c['description']).'</textarea></td><td><input name="metric_label" value="'.e($c['metric_label']).'"></td><td><select name="query_key">'; foreach($queries as $k=>$label) echo '<option value="'.e($k).'" '.($c['query_key']===$k?'selected':'').'>'.e($label).'</option>'; echo '</select><input name="chart_type" value="'.e($c['chart_type']).'"></td><td><select name="required_tier_id"><option value="">Public</option>'; foreach($tiers as $t) echo '<option value="'.(int)$t['id'].'" '.((int)$c['required_tier_id']===(int)$t['id']?'selected':'').'>'.e($t['name']).'</option>'; echo '</select></td><td><input name="display_order" value="'.(int)$c['display_order'].'"></td><td><select name="is_active"><option value="1" '.($c['is_active']?'selected':'').'>Yes</option><option value="0" '.(!$c['is_active']?'selected':'').'>No</option></select></td><td><button class="btn mini">Save</button></td></form></tr>'; } echo '</tbody></table></div>';
}
function render_admin_imports(): void {
    $module=getv('module','airlines'); $cfg=module_config($module);
    echo '<div class="admin-head"><div><div class="eyebrow">Data Operations</div><h1>Imports / Exports</h1><p>Import selected modules, export filtered records, or export a full CSV ZIP backup.</p></div><div class="hero-actions"><a class="btn primary" href="?page=export_all">Export whole database ZIP</a></div></div>';
    echo '<div class="panel-grid"><article class="panel"><h3>Import CSV to module</h3><form method="post" enctype="multipart/form-data" class="stack-form">'.csrf_field().'<input type="hidden" name="action" value="admin_import_csv"><label>Module<select name="module">'; foreach(modules() as $k=>$c) echo '<option value="'.e($k).'" '.($k===$module?'selected':'').'>'.e($c['label']).' — '.e($k).'</option>'; echo '</select></label><label>Import mode<select name="import_mode"><option value="append">Append new records</option><option value="truncate_append">Clear module then import</option></select></label><label>CSV file<input type="file" name="csv_file" accept=".csv" required></label><label>Batch notes<textarea name="notes" placeholder="Source, assumptions, or cleanup notes"></textarea></label><button class="btn ink">Import CSV</button></form><p class="muted">Headers are matched to editable field names. Failed rows are sent to Data Quality → Staging Records.</p></article>';
    echo '<article class="panel"><h3>Export selected module</h3><form method="get" class="stack-form"><input type="hidden" name="page" value="export"><label>Module<select name="module">'; foreach(modules() as $k=>$c) echo '<option value="'.e($k).'" '.($k===$module?'selected':'').'>'.e($c['label']).'</option>'; echo '</select></label><label>Search filter<input name="q" placeholder="Optional keyword"></label><label>Country filter<input name="country" placeholder="KE, UG, TZ..."></label><button class="btn ink">Export filtered CSV</button></form><p class="muted">Admin exports respect filters and are logged in <strong>Export Logs</strong>.</p></article></div>';
    echo '<section class="panel"><h3>Phase 4 source importers</h3><p class="muted">For large source folders and country ZIP imports, run the CLI importer so long-running jobs are not interrupted by the browser.</p><div class="code-grid"><code>php scripts/importers/phase4_import.php --group=all</code><code>php scripts/importers/phase4_import.php --group=country --country=KE</code><code>php scripts/importers/phase4_import.php --group=aircraft</code><code>php scripts/importers/phase4_import.php --group=reference</code><code>php scripts/importers/phase4_import.php --group=commercial</code><code>php scripts/importers/phase4_import.php --group=infrastructure</code></div><p class="muted">Groups available: global, country, aircraft, reference, commercial, iata-iosa, gds, infrastructure. Use <code>--dry-run</code>, <code>--mode=replace</code>, <code>--limit=1000</code> or <code>--store-raw=1</code> when needed.</p></section>';
    echo '<section class="panel"><h3>Module export shortcuts</h3><div class="module-grid compact">'; foreach(module_groups() as $group=>$keys){ echo '<article class="module-group"><h3>'.e($group).'</h3><div>'; foreach($keys as $k){ $c=module_config($k); if($c) echo '<a class="module-chip" href="?page=export&module='.e($k).'"><b>CSV</b><span>'.e($c['label']).'</span><em>'.nfmt(module_count($k)).'</em></a>'; } echo '</div></article>'; } echo '</div></section>';
    $logs=rows('SELECT * FROM import_batches ORDER BY started_at DESC LIMIT 20'); echo '<h3>Recent import batches</h3>'.render_table($logs,$logs?array_keys($logs[0]):[],null);
}
function render_admin_quality(): void {
    $staging=rows('SELECT * FROM staging_import_records ORDER BY created_at DESC LIMIT 100'); $batches=rows('SELECT * FROM import_batches ORDER BY started_at DESC LIMIT 50'); $exports=rows('SELECT el.*, u.email FROM export_logs el LEFT JOIN users u ON u.id=el.user_id ORDER BY el.created_at DESC LIMIT 50'); $empty=admin_empty_modules();
    $needs=admin_safe_count("SELECT COUNT(*) FROM staging_import_records WHERE status IN ('pending','needs_review','conflict')"); $failed=admin_safe_count("SELECT COUNT(*) FROM import_batches WHERE status IN ('failed','needs_review')");
    echo '<div class="admin-head"><div><div class="eyebrow">Data Quality</div><h1>Review queue</h1><p>Imports should not blindly become trusted aviation intelligence. Review failed rows, empty modules and export usage.</p></div></div><div class="admin-kpi-grid">'.admin_metric('Rows needing review',$needs,'Staging queue',$needs?'warning':'success').admin_metric('Import issues',$failed,'Failed/needs-review batches',$failed?'danger':'success').admin_metric('Empty modules',count($empty),'Awaiting data').admin_metric('Export logs',count($exports),'Recent exports').'</div>';
    echo '<section class="panel"><h3>Empty / awaiting-data modules</h3>'; if(!$empty) echo '<p class="muted">No empty configured modules.</p>'; else { echo '<div class="admin-empty-list">'; foreach($empty as $k=>$c) echo '<a href="?page=admin&tab=records&module='.e($k).'"><strong>'.e($c['label']).'</strong><small>'.e($k).'</small></a>'; echo '</div>'; } echo '</section>';
    echo '<section class="panel"><h3>Staging records</h3>'; if(!$staging) echo '<p class="muted">No staged import issues.</p>'; else { echo '<div class="table-wrap"><table><thead><tr><th>Module</th><th>Row</th><th>Status</th><th>Issue</th><th>Action</th></tr></thead><tbody>'; foreach($staging as $r){ echo '<tr><form method="post">'.csrf_field().'<input type="hidden" name="action" value="admin_update_staging"><input type="hidden" name="staging_id" value="'.(int)$r['id'].'"><td>'.e($r['module_key']).'</td><td>'.e($r['source_row_number']).'</td><td><select name="status">'; foreach(['pending','needs_review','accepted','rejected','duplicate','conflict'] as $st) echo '<option '.($r['status']===$st?'selected':'').'>'.$st.'</option>'; echo '</select></td><td>'.display_value($r['issue_summary']).'</td><td><button class="btn mini">Update</button></td></form></tr>'; } echo '</tbody></table></div>'; } echo '</section>';
    echo '<section class="panel"><h3>Import batches</h3>'.render_table($batches,$batches?array_keys($batches[0]):[],null).'</section><section class="panel"><h3>Export logs</h3>'.render_table($exports,$exports?array_keys($exports[0]):[],null).'</section>';
}
function render_admin_tasks(): void { $tasks=rows('SELECT * FROM admin_tasks ORDER BY sort_order,id'); echo '<div class="admin-head"><div><div class="eyebrow">Readiness</div><h1>100% implementation checklist</h1><p>Track launch readiness from inside the admin console.</p></div></div><div class="table-wrap"><table><thead><tr><th>Category</th><th>Task</th><th>Status</th><th>Priority</th><th>Action</th></tr></thead><tbody>'; foreach($tasks as $t){ echo '<tr><form method="post">'.csrf_field().'<input type="hidden" name="action" value="admin_update_task"><input type="hidden" name="task_id" value="'.(int)$t['id'].'"><td>'.e($t['category']).'</td><td><strong>'.e($t['task_title']).'</strong><p>'.e($t['description']).'</p></td><td><select name="status">'; foreach(['pending','in_progress','done','blocked'] as $st) echo '<option '.($t['status']===$st?'selected':'').'>'.$st.'</option>'; echo '</select></td><td>'.e($t['priority']).'</td><td><button class="btn mini">Update</button></td></form></tr>'; } echo '</tbody></table></div>'; }
?>
