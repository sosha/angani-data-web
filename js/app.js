(function(){const slider=document.getElementById('heroSlider');if(!slider)return;const slides=slider.querySelectorAll('.hero-slide');const dots=document.querySelectorAll('.hero-slider-dot');let idx=0;function show(i){slides.forEach((s,j)=>{s.classList.toggle('active',j===i)});dots.forEach((d,j)=>{d.classList.toggle('active',j===i)});idx=i}if(slides.length<2)return;dots.forEach(d=>d.addEventListener('click',()=>show(parseInt(d.dataset.index))));setInterval(()=>show((idx+1)%slides.length),5000)})();const menuToggle=document.getElementById('menuToggle');const mainNav=document.getElementById('mainNav');if(menuToggle&&mainNav){menuToggle.addEventListener('click',()=>{const open=mainNav.classList.toggle('open');menuToggle.setAttribute('aria-expanded',open?'true':'false')});mainNav.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>mainNav.classList.remove('open')))}const year=document.getElementById('year');if(year)year.textContent=new Date().getFullYear();const topBtn=document.getElementById('toTop');if(topBtn){window.addEventListener('scroll',()=>topBtn.classList.toggle('show',window.scrollY>500));topBtn.addEventListener('click',()=>window.scrollTo({top:0,behavior:'smooth'}))}document.querySelectorAll('[data-auto-submit]').forEach(el=>el.addEventListener('change',()=>el.form.submit()));document.querySelectorAll('.record-card').forEach((card,i)=>{card.style.animationDelay=(i%24*18)+'ms'});const searchToggle=document.getElementById('searchToggle');const searchOverlay=document.getElementById('searchOverlay');if(searchToggle&&searchOverlay){searchToggle.addEventListener('click',()=>{const open=searchOverlay.hidden;searchOverlay.hidden=!open;searchToggle.classList.toggle('open',!open);searchToggle.setAttribute('aria-expanded',!open);if(!open)setTimeout(()=>searchOverlay.querySelector('.search-overlay-input')?.focus(),100)});document.addEventListener('click',e=>{if(!searchOverlay.hidden&&!searchToggle.contains(e.target)&&!searchOverlay.contains(e.target)){searchOverlay.hidden=true;searchToggle.classList.remove('open');searchToggle.setAttribute('aria-expanded','false')}});searchOverlay.querySelector('form').addEventListener('submit',()=>{searchOverlay.hidden=true;searchToggle.classList.remove('open');searchToggle.setAttribute('aria-expanded','false')})}